<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CreditPro</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav class="navbar">
    <div class="container">
      <a href="/" class="logo">
        <div class="logo-icon">💳</div>
        <span>CreditPro</span>
      </a>
      <ul class="nav-links">
        <li><a href="/">Главная</a></li>
        <li><a href="/calculator">Калькулятор</a></li>
        <li><a href="/clients" class="active">Клиенты</a></li>
        <li><a href="/credits">Кредиты</a></li>
      </ul>
    </div>
  </nav>

  <div class="container main-content">
    <div class="page-header">
      <h1>Клиенты</h1>
      <a href="/clients/new" class="btn btn-primary">Добавить клиента</a>
    </div>

    <% if (clients.length === 0) { %>
      <div class="empty-state">
        <p>Клиенты не найдены</p>
        <a href="/clients/new" class="btn">Добавить первого клиента</a>
      </div>
    <% } else { %>
      <div class="table-container">
        <table class="data-table">
          <thead>
            <tr>
              <th>ФИО</th>
              <th>Паспорт</th>
              <th>Телефон</th>
              <th>Email</th>
              <th>Доход</th>
              <th>Действия</th>
            </tr>
          </thead>
          <tbody>
            <% clients.forEach(client => { %>
              <tr>
                <td><%= client.lastName %> <%= client.firstName %> <%= client.middleName || '' %></td>
                <td><%= client.passportSeries %></td>
                <td><%= client.phoneNumber %></td>
                <td><%= client.email %></td>
                <td><%= client.monthlyIncome.toLocaleString() %> сум</td>
                <td class="actions">
                  <a href="/clients/<%= client._id %>" class="btn-small">Просмотр</a>
                  <a href="/clients/<%= client._id %>/edit" class="btn-small">Изменить</a>
                </td>
              </tr>
            <% }); %>
          </tbody>
        </table>
      </div>
    <% } %>
  </div>

  <footer class="footer">
    <div class="container">
      <p>&copy; 2024 CreditPro. Система управления кредитами.</p>
    </div>
  </footer>
</body>
</html>
